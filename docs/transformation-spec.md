Got it — so the directive is: **preserve all animations, scroll effects, the iPhone parallax, the video embed behavior, the image treatment** — just swap the content layer on top of the existing template's bones. Here's the full CLEAR-structured V0 prompt:

---

## V0 Agent Prompt — Clinic Autopilot Transformation

---

> **You are a V0 frontend developer helping transform an existing landing page template for a new business.**
>
> ---
>
> ### C — CLARITY
>
> **Problem:** The "Homie" real estate rental template needs its content, copy, branding, and color palette replaced to serve a healthcare automation agency called Clinic Autopilot. The visual structure, scroll behavior, animations, and layout patterns must remain completely intact.
>
> **Objective:** Produce a pixel-faithful transformation of the Homie template where every animation, parallax effect, scroll interaction, the iPhone mockup reveal, the floating nav card, the stat counter block, the image section, and the video embed behavior are all preserved exactly as-is — only the words, colors, icons, and images change.
>
> **Scope — what changes:**
> - All text/copy, headlines, nav labels, button labels, stat numbers, section descriptions
> - Brand name, logo text, and logo icon
> - Color palette (navy, white, teal accent — details below)
> - Section-specific images where they show real estate content (replace with clinical/neutral equivalents)
> - The screenshot inside the iPhone mockup (replace with an intake form or clinic dashboard UI)
> - Any real estate-specific UI (property listings grid, rental revenue widget, property names)
>
> **Scope — what does NOT change:**
> - The iPhone scroll/parallax reveal animation
> - The floating nav card behavior and sticky scroll
> - The stat block layout and any count-up animations
> - The mission section image treatment (full-bleed rounded card with text overlay) — keep the layout, swap the image to something clinical/calm (soft light, clean interior, or use a deep navy gradient with noise texture if no image fits)
> - The features grid layout (3-up icon + title + body)
> - The dashboard/property section layout — repurpose as "How It Works" steps, keep the split layout and any widget card styling
> - The dual-CTA final section layout
> - All scroll-triggered reveals, fade-ins, stagger animations
> - All font choices (keep the existing serif display + sans body pairing)
> - Section spacing, padding rhythm, and overall scroll pacing
>
> ---
>
> ### L — LOGIC
>
> **Step 1 — Rebrand the nav:**
> - Logo icon: replace house icon with a small `+` or cross (medical) SVG
> - Logo text: `Clinic Autopilot`
> - Nav links: